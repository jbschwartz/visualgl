from visualgl.exceptions import VisualError


class LayoutError(VisualError):
    """Raised when an error occurs during layout initialization and creation."""


class WindowError(VisualError):
    """Raised when an error occurs during window initialization and creation."""
